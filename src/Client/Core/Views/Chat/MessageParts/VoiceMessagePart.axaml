<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shared="using:Core.Views.Controls.Shared"
             xmlns:parts="using:Core.Views.Chat.MessageParts.Shared"
             xmlns:vmChat="using:Core.ViewModels.Chat"
             x:Class="Core.Views.Chat.MessageParts.VoiceMessagePart"
             x:DataType="vmChat:MessageViewModel"
             x:Name="VoiceRoot">

    <StackPanel>

        <!-- Удалённое состояние -->
        <StackPanel IsVisible="{Binding IsDeleted}">
            <TextBlock Text="{Binding DisplayContent}"
                       FontStyle="Italic"
                       Foreground="{DynamicResource TextSecondary}"
                       TextWrapping="Wrap"
                       FontSize="15"
                       LineHeight="22"/>
            <parts:MessageMetaBlock
                HorizontalAlignment="Right"
                Margin="0,-2,0,0"
                Opacity="0.7"/>
        </StackPanel>

        <!-- Плеер -->
        <Border Classes="VoicePlayerContainer"
                IsVisible="{Binding !IsDeleted}"
                MinWidth="180">
            <StackPanel Spacing="4">

                <Grid ColumnDefinitions="46,*"
                      VerticalAlignment="Center">

                    <!-- Кнопки управления — все в одной ячейке [0,0] -->
                    <Panel Grid.Column="0"
                           Width="46"
                           Height="46"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center">

                        <Button Classes="VoicePlayBtn"
                                Width="46" Height="46"
                                Command="{Binding PlayVoiceCommand}"
                                IsVisible="{Binding ShowPlayButton}"
                                ToolTip.Tip="Воспроизвести">
                            <PathIcon Data="{StaticResource PlayIcon}" Width="20" Height="20"/>
                        </Button>

                        <Button Classes="VoicePlayBtn"
                                Width="46" Height="46"
                                Command="{Binding PauseVoiceCommand}"
                                IsVisible="{Binding ShowPauseButton}"
                                ToolTip.Tip="Пауза">
                            <PathIcon Data="{StaticResource PauseIcon}" Width="20" Height="20"/>
                        </Button>

                        <Button Classes="VoicePlayBtn"
                                Width="46" Height="46"
                                Command="{Binding PlayVoiceCommand}"
                                IsVisible="{Binding ShowResumeButton}"
                                ToolTip.Tip="Продолжить">
                            <PathIcon Data="{StaticResource PlayIcon}" Width="20" Height="20"/>
                        </Button>

                        <Border Classes="VoiceLoadingContainer"
                                Width="46" Height="46"
                                IsVisible="{Binding IsVoiceLoading}">
                            <shared:CircularProgress
                                Width="22" Height="22"
                                IsIndeterminate="True"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </Panel>

                    <!-- Правая часть: waveform + нижняя строка -->
                    <StackPanel Grid.Column="1"
                                Margin="10,0,0,0"
                                Spacing="2"
                                VerticalAlignment="Center">

                        <!-- Waveform -->
                        <shared:WaveformView
                            Height="32"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            ClipToBounds="True"
                            Background="Transparent"
                            Cursor="Hand"
                            Waveform="{Binding VoiceWaveform}"
                            Progress="{Binding VoicePositionPercent, Mode=OneWay}"
                            SeekCommand="{Binding SeekVoiceCommand}"/>

                        <!-- Время + мета -->
                        <Grid ColumnDefinitions="Auto,*"
                              VerticalAlignment="Center">

                            <Border Grid.Column="0"
                                    Classes="VoiceTimeBadge"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center">
                                <TextBlock Classes="VoiceTime">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0} / {1}">
                                            <Binding Path="VoicePositionText"/>
                                            <Binding Path="VoiceDurationFormatted"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </Border>

                            <parts:MessageMetaBlock
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="4,0,0,0"
                                Opacity="0.7"/>
                        </Grid>

                    </StackPanel>
                </Grid>

                <!-- Ошибка -->
                <TextBlock Text="{Binding VoiceError}"
                           Classes="VoiceError"
                           TextWrapping="Wrap"
                           IsVisible="{Binding VoiceError,
                               Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>

            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>